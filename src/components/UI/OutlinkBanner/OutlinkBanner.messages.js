import { defineMessages } from 'react-intl';

export default defineMessages({
  seeSpeechSynthesisSupport: {
    id: 'app.seeSpeechSynthesisSupport',
    defaultMessage: 'See which languages and voices your browser supports'
  }
});
